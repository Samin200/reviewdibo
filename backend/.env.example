# PostgreSQL connection string used by the app (async driver) and Alembic.
# For the async app, SQLAlchemy uses the asyncpg driver. Alembic uses a sync
# driver derived from this URL automatically.
DATABASE_URL=postgresql://user:password@localhost:5432/reviewdibo
